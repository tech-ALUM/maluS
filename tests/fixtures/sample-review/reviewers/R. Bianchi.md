# Sensor Interface Requirements

## 3.2.1 Timeouts

The acquisition timeout shall be configurable by the operator.

## 3.3 Logging

All measurements are written to disk in CSV format. {SUGG: "disk" -> "the configured store"} {COMM|type=editorial: "measurements" is vague; name the signals}
